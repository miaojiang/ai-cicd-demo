import test from "node:test";
import assert from "node:assert/strict";
import { computePayoutCents, isAuthorized } from "../src/payments.js";

test("placeholder", () => {
  // TODO: add tests once implementation is done
  assert.ok(true);
});
